apiVersion: v1
kind: Service
metadata:
  name: users-ws
  labels:
    app: users-ws
    spring-boot: "true"
spec:
  ports:
    - port: 80
      targetPort: 3333
  #      protocol: TCP
  selector:
    app: users-ws
  type: ClusterIP