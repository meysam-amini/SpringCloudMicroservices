apiVersion: v1
kind: Service
metadata:
  name: backoffice
  labels:
    app: backoffice
    spring-boot: "true"
spec:
  ports:
    - port: 9999
      protocol: TCP
  selector:
    app: department
  type: ClusterIP