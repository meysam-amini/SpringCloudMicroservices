apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  labels:
    app: api-gateway
    spring-boot: "true"
spec:
  ports:
    - port: 80
      targetPort: 8082
#      protocol: TCP
  selector:
    app: api-gateway
  type: ClusterIP