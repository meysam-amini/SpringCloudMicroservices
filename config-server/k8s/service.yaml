apiVersion: v1
kind: Service
metadata:
  name: config-server
  labels:
    app: config-server
    spring-boot: "true" #is set for Spring Boot Admin discovery purposes
spec:
  ports:
    - port: 8012
      targetPort: 8012
  #      protocol: TCP
  selector:
    app: config-server
  type: ClusterIP